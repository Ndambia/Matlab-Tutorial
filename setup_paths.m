%% SETUP PATHS FOR THE MATLAB TRAINING PACKAGE
% Run this once after opening the package folder in MATLAB.

clc;
clear;

root_folder = pwd;
addpath(genpath(root_folder));

disp('All package subfolders added to the MATLAB path.');
disp('You can now open any session folder and run the scripts.');
